# Managed by Puppet

auth sufficient pam_rootok.so no_warn
auth sufficient pam_self.so no_warn
auth requisite pam_group.so no_warn group=wheel,su root_only fail_safe
auth include system

account include system

session required pam_permit.so
